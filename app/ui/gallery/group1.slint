// 展示页 组 1：Button、IconButton、Select、Modal（ui.md §5）。只用于开发手测，不写业务逻辑、不用 @tr。
import { Theme } from "../common/theme.slint";

export component GalleryGroup1 {
    VerticalLayout {
        spacing: 16px;

        Text {
            text: "组 1：Button、IconButton、Select、Modal";
            font-size: 18px;
            color: Theme.foreground;
        }
    }
}

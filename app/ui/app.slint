// build.rs 只编译这一个入口，所有要给 Rust 用的组件从这里导出。
export { RenderCheck } from "settings/render_check.slint";
export { Tray } from "tray.slint";
export { PopButton } from "pop_button.slint";
export { PopResult, ResultRow } from "pop_result.slint";
export { EntryExample, EntryExplain, EntryView, EntryVocab } from "common/entry_view.slint";
export { Gallery } from "gallery.slint";
export { Theme } from "common/theme.slint";


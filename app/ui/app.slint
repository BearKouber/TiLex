// build.rs 只编译这一个入口，所有要给 Rust 用的组件从这里导出。
export { RenderCheck, RoundCheck } from "settings/render_check.slint";
export { Tray } from "tray.slint";
export { PopButton } from "pop_button.slint";
export { PopResult, ResultRow } from "pop_result.slint";

